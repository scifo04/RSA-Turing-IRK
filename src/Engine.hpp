#include <iostream>
#include <vector>
using namespace std;

class Engine {
    private:
        string text;
        int p;
        int q;
        int n;
        int toitentN;
        int e;
        int d;
        vector<string> plainChar;
        vector<string> serializedChar;
    public:
        Engine();
};